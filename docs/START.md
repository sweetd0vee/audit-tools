# С чего начать писать

**v0.0.1 уже есть.** Это демо-версия агента: библиотека НПА, саммари, программа, гипотезы, мнение, заключение. Не изобретать агента заново и не «чуть причесать» чат.

Фронтенд **не пишем**. `frontend/` не трогаем: ни фичи, ни вёрстку. Это лаборатория API, не продукт.

Как аудитор этим пользуется: [GUIDE.md](GUIDE.md). Настройка Open WebUI: [OPENWEBUI.md](OPENWEBUI.md). Шпаргалка команд: [AUDITOR.md](AUDITOR.md). Что добавлять после 0.0.1: [PLAN.md](PLAN.md).

---

## Что уже есть (не изобретать)

| Есть в v0.0.1 | Не начинать с |
|---|---|
| FastAPI: кейс → propose → select → download → index → ask → brief / total / program / hypotheses / opinion / conclusion | нового React, нового чата, второго агента |
| Pipe «Аудитор» 0.0.1: HITL, `вопрос …` = RAG, иначе обычный чат | LangGraph, native tools как основной путь |
| `docker-compose.yml`: SearXNG, Open WebUI, backend; frontend только `--profile lab` | своего UI |
| `searxng/settings.yml`, known_sources, extra_titles, ручной URL | нового поисковика |
| Ollama на хосте | LLM в Docker «на вырост» |
| Word: саммари, total, программа, мнение, заключение; Excel: гипотезы | редактора WP, портала заключения |

Писать нужно только то, чего нет в [PLAN.md](PLAN.md): цитата/eval/trail (v0.1), засев Pipe (v0.2), потом факт / тесты / WP.

---

## Сейчас — в этом порядке

### 1. Качество цитаты (v0.1)

1. Перед sync: `.txt` → markdown `## Статья N` (чанкер уже режет по статье — довести до файла, который едет в Knowledge).
2. `eval_rag.py`: 20 вопросов, гонять после смены embedding.
3. Trail: какой чанк ушёл в `/knowledge/ask`.
4. Правки промптов — в `docs/prompts/`, сводка в [Промпты агента.docx](Промпты агента.docx).

Не писать: тесты проверки, WP, DuckDB, users, Postgres. Сначала цитата из утверждённого акта стабильна.

### 2. Коробка (v0.2)

- Засев Pipe при `compose up` без вставки в Admin.
- Одна страница «поставил → открыл :3000».
- Корневой `.env.example` для compose.

### 3. Не раньше, чем 1 и 2 зелёные

Данные клиента (v0.4), `propose_tests` и шаблон WP (v0.5). Программа проверки Word уже закрывает черновик СВА — не дублировать её SPA. Мнение и заключение уже в 0.0.1.

---

## Ежедневное правило за клавиатурой

Перед новым файлом:

1. Это `.tsx` / новый экран? Стоп.
2. Это уже умеет Open WebUI настройкой? Стоп, пишем в `seed/`.
3. Это вызов существующего API из чата? Ветка в Pipe (`audit_agent.py`) + эндпоинт в `backend/app/`.
4. Это HITL, allowlist, кейс, trail, нарезка статьи? `backend/app/`.
5. Это «ещё одна фаза агента, которая уже есть в 0.0.1»? Стоп.

---

## Команды, чтобы сесть сегодня

Ollama на хосте, как в compose. Затем:

```powershell
cd c:\Users\audit\Work\Arina\2026\audit-tools
docker compose up -d --build
```

Открыть:

- продукт: http://localhost:3000 — модель **Аудитор** (Pipe 0.0.1), гайд [GUIDE.md](GUIDE.md)
- вопрос к базе: `вопрос …` (без префикса это обычный чат, не цитата)
- API: http://localhost:8100/docs

Лабораторный UI http://localhost:5174 не открывать как «место работы».

После правки Pipe — заново вставить `audit_agent.py` в Admin → Функции ([AGENT.md](../seed/openwebui/AGENT.md)).

---

## Готово, когда (планка после 0.0.1)

Демо 0.0.1 уже показывает человеку в чате:

1. Чат «Аудитор»: HITL на НПА, саммари, программу, гипотезы, мнение, заключение.
2. `вопрос …` — с цитатой; обычный текст — не цитата.
3. Никто не открывал Vite.

Ещё нет (это и есть план):

4. Один URL, Pipe уже в списке моделей **без** ручного Admin.
5. На вопросе, которого нет в библиотеке — стабильный отказ (eval, не «промпт так говорит»).
6. Данные клиента, тесты, WP.
