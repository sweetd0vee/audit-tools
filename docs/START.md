# С чего начать писать

Фронтенд **не пишем**. `frontend/` не трогаем: ни фичи, ни «чуть причесать». Это лаборатория API, не продукт.

Первый код — не новые экраны и не тесты/WP. Чат уже ведёт проверку. Дальше — **цитата из утверждённого акта** и коробка без ручного Admin.

Как аудитор этим пользуется: [GUIDE.md](GUIDE.md). Настройка Open WebUI: [OPENWEBUI.md](OPENWEBUI.md). Шпаргалка команд: [AUDITOR.md](AUDITOR.md). Дорожная карта: [PLAN.md](PLAN.md).

---

## Что уже есть (не изобретать)

| Есть | Не начинать с |
|---|---|
| FastAPI: кейс → propose → select → download → index → ask → brief / total / program | нового React, нового чата |
| Pipe «Аудитор»: HITL, `вопрос …` = RAG, иначе обычный чат | второго агента / LangGraph |
| `docker-compose.yml`: SearXNG, Open WebUI, backend; frontend только `--profile lab` | своего UI |
| `searxng/settings.yml`, known_sources, extra_titles, ручной URL | нового поисковика |
| Ollama на хосте | LLM в Docker «на вырост» |
| Word: саммари по базе, total из знаний модели, программа проверки | редактора WP |

Писать нужно только то, чего нет: засев Pipe без Admin, markdown-статьи перед sync, eval RAG, trail. Агент и HITL уже есть.

---

## Сейчас — в этом порядке

### 1. Качество цитаты (этап 3 плана)

1. Перед sync: `.txt` → markdown `## Статья N` (чанкер уже режет по статье — довести до файла, который едет в Knowledge).
2. `eval_rag.py`: 20 вопросов, гонять после смены embedding.
3. Trail: какой чанк ушёл в `/knowledge/ask`.

Не писать: тесты проверки, WP, DuckDB, users, Postgres. Сначала цитата из утверждённого акта стабильна.

### 2. Коробка (хвост этапа 1 и 8)

- Засев Pipe при `compose up` без вставки в Admin.
- Одна страница «поставил → открыл :3000».
- Корневой `.env.example` для compose.

### 3. Не раньше, чем 1 зелёный

Данные клиента, `propose_tests`, шаблон WP. Программа проверки Word уже закрывает черновик СВА — не дублировать её SPA.

---

## Ежедневное правило за клавиатурой

Перед новым файлом:

1. Это `.tsx` / новый экран? Стоп.
2. Это уже умеет Open WebUI настройкой? Стоп, пишем в `seed/`.
3. Это вызов существующего API из чата? Ветка в Pipe (`audit_agent.py`) + эндпоинт в `backend/app/`.
4. Это HITL, allowlist, кейс, trail, нарезка статьи? `backend/app/`.

---

## Команды, чтобы сесть сегодня

Ollama на хосте, как в compose. Затем:

```powershell
cd c:\Users\audit\Work\Arina\2026\audit-tools
docker compose up -d --build
```

Открыть:

- продукт: http://localhost:3000 — модель **Аудитор**, гайд [GUIDE.md](GUIDE.md)
- вопрос к базе: `вопрос …` (без префикса это обычный чат, не цитата)
- API: http://localhost:8100/docs

Лабораторный UI http://localhost:5174 не открывать как «место работы».

---

## Готово, когда

Можно показать человеку без репозитория:

1. Один URL, Pipe уже в списке моделей.
2. Чат «Аудитор» без настройки RAG руками.
3. Список НПА нельзя скачать без «утверждаю».
4. `саммари` отдаёт Word со ссылками на статьи; `программа проверки` — черновик программы.
5. `вопрос …` — с цитатой; вопроса нет в библиотеке — отказ.
6. `гипотезы` — Excel-чеклист гипотез для проверки.
7. Никто не открывал Vite.
